# MACKHAN monorepo — service env files (created in later tasks):
#   backend/.env.example  → backend/.env
#   ml-service/.env.example → ml-service/.env
# Full variable reference: docs/PLAN.md §4

COMPOSE_PROJECT_NAME=mackhan
